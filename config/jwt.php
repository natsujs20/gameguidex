<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Clave secreta
    |--------------------------------------------------------------------------
    |
    | Se obtiene desde JWT_SECRET en el archivo .env.
    |
    */

    'secret' => env('JWT_SECRET'),

    /*
    |--------------------------------------------------------------------------
    | Duración del token
    |--------------------------------------------------------------------------
    |
    | Tiempo de duración expresado en minutos.
    |
    */

    'ttl' => (int) env('JWT_TTL', 60),

    /*
    |--------------------------------------------------------------------------
    | Algoritmo de firma
    |--------------------------------------------------------------------------
    */

    'algorithm' => env(
        'JWT_ALGORITHM',
        'HS256'
    ),

];